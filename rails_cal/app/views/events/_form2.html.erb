<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= f.hidden_field :activity_id, {:value => @current_activity_id} %>

  <div class="field">
    <%= f.label :activity_name, "Activity Name:" %><br />
    <%= f.text_field :activity_name, {:value => @current_activity} %>

  <div class="field">
    <%= f.label :name, "Event Name:" %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :many_dates, "Event Dates:" %><br />
    <%= f.text_field :many_dates %>
  </div>

   <div class="field">
    <%= f.label :many_start_times, "Default Start Time:" %><br />
    <%= f.time_select :many_start_times,
    :minute_step => 5 %>
  </div>


   <div class="field">
    <%= f.label :many_end_times, "Default End Time:" %><br />
    <%= f.time_select :many_end_times,
    :minute_step => 5 %>
  </div>

  <%= text_field_tag(:save_dates, "Click here to save dates- form2") %>


  <div class="actions">
    <%= f.submit "NEXT" %>
  </div>
<% end %>


<%= params %>
<br/>


params into create event

{"activity_id"=>"81",
 "activity_name"=>"T11", 
 "name"=>"T11 Event", 
 "many_dates"=>"05/15/2013",
  "many_start_times(1i)"=>"2013", 
  "many_start_times(2i)"=>"5", 
  "many_start_times(3i)"=>"12", 
  "many_start_times(4i)"=>"14", 
  "many_start_times(5i)"=>"00",
   "many_end_times(1i)"=>"2013",
    "many_end_times(2i)"=>"5", 
    "many_end_times(3i)"=>"12",
     "many_end_times(4i)"=>"14",
      "many_end_times(5i)"=>"00"}


